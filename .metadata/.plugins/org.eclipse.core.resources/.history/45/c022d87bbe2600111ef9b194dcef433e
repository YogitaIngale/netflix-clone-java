package com.yogita.netflix.repository;

import com.yogita.netflix.model.UploadLog;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface UploadLogRepository extends JpaRepository<UploadLog, Long> {

    // Get all logs by admin
    List<UploadLog> findByAdminId(Long adminId);
}